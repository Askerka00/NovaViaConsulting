---
// Nova Via Consulting Banner Component
---
<section class="hero-banner">
  <div class="hero-content">
    <p class="subtitle">До $500 000 получено в виде грантов и скидок</p>
    <h1 class="main-title">Nova Via Consulting</h1>
    <p class="description">
      Предоставляем создание души мира с полной поддержкой на всех
      этапах от выбора программы до защитника за границей!
    </p>
    <button data-scroll-to="form" class="cta-button">Бесплатная консультация</button>
  </div>
</section>

<style>
  .hero-banner {
    height: 500px;
    position: relative;
    background: linear-gradient(
      rgba(0, 0, 0, 0.6),
      rgba(0, 0, 0, 0.4)
    ), url('/src/assets/images/czech.jpg');
    background-size: cover;
    background-position: center;
    display: flex;
    align-items: center;
    padding: 0 150px;
    color: white;
    overflow: hidden;
    margin-top: 128px;
  }
  
  .hero-content {
    max-width: 1240px;
    width: 100%;
    margin: 0 auto;
    z-index: 2;
    animation: slideUp 1s ease-out;
  }
  
  .subtitle {
    font-size: 16px;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.9);
    letter-spacing: 0.5px;
    margin-left: 4px;
  }
  
  .main-title {
    font-size: 50px;
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 24px;
    margin-top: 12px;
    background: linear-gradient(135deg, #fff 0%, #f0f0f0 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  
  .description {
    font-size: 16px;
    line-height: 1.5;
    color: rgba(255, 255, 255, 0.85);
    margin-bottom: 36px;
    max-width: 520px;
  }
  
  .cta-button {
    padding: 18px 36px;
    background: linear-gradient(135deg, #ff6b00 0%, #ff8533 100%);
    color: white;
    border: none;
    border-radius: 50px;
    font-family: inherit;
    font-weight: 600;
    font-size: 16px;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 8px 32px rgba(255, 107, 0, 0.3);
  }
  
  .cta-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 40px rgba(255, 107, 0, 0.4);
  }
  
  .floating-elements {
    position: absolute;
    top: 0;
    right: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
  }
  /* Адаптив для экранов до 1440px */
  @media (max-width: 1440px) {
    .hero-banner {
      padding: 0 130px;
      margin-top: 100px;
    }
  }

  /* Адаптив для экранов до 1200px */
  @media (max-width: 1200px) {
    .hero-banner {
      padding: 0 100px;
      height: 450px;
    }
    
    .main-title {
      font-size: 44px;
    }
  }

  /* Адаптив для экранов до 900px */
  @media (max-width: 900px) {
    .hero-banner {
      padding: 0 40px;
      height: 400px;
      margin-top: 80px;
    }
    
    .main-title {
      font-size: 38px;
      margin-bottom: 20px;
    }
    
    .subtitle {
      font-size: 15px;
    }
    
    .description {
      font-size: 15px;
      margin-bottom: 30px;
      max-width: 480px;
    }
    
    .cta-button {
      padding: 16px 32px;
      font-size: 15px;
    }
  }

  /* Адаптив для экранов до 768px */
  @media (max-width: 768px) {
    .hero-banner {
      padding: 0 20px;
      height: 380px;
      margin-top: 60px;
      text-align: center;
      justify-content: center;
    }
    
    .main-title {
      font-size: 32px;
      margin-bottom: 18px;
    }
    
    .subtitle {
      font-size: 14px;
      margin-left: 0;
    }
    
    .description {
      font-size: 14px;
      line-height: 1.4;
      margin-bottom: 28px;
      max-width: 100%;
    }
    
    .hero-content {
      max-width: 100%;
    }
    
    .cta-button {
      padding: 14px 28px;
      font-size: 14px;
    }
  }

  /* Адаптив для экранов до 600px */
  @media (max-width: 600px) {
    .hero-banner {
      height: 350px;
      padding: 0 15px;
      margin-top: 50px;
    }
    
    .main-title {
      font-size: 28px;
      margin-bottom: 16px;
      margin-top: 10px;
    }
    
    .subtitle {
      font-size: 13px;
    }
    
    .description {
      font-size: 13px;
      margin-bottom: 25px;
    }
    
    .cta-button {
      padding: 12px 24px;
      font-size: 13px;
      border-radius: 40px;
    }
  }

  /* Адаптив для экранов до 480px */
  @media (max-width: 480px) {
    .hero-banner {
      height: 320px;
      padding: 0 10px;
      margin-top: 40px;
    }
    
    .main-title {
      font-size: 24px;
      margin-bottom: 14px;
    }
    
    .subtitle {
      font-size: 12px;
    }
    
    .description {
      font-size: 12px;
      line-height: 1.3;
      margin-bottom: 20px;
    }
    
    .cta-button {
      padding: 10px 20px;
      font-size: 12px;
      border-radius: 30px;
    }
  }

  /* Адаптив для очень маленьких экранов */
  @media (max-width: 360px) {
    .hero-banner {
      height: 300px;
      padding: 0 8px;
    }
    
    .main-title {
      font-size: 22px;
      margin-bottom: 12px;
    }
    
    .subtitle {
      font-size: 11px;
    }
    
    .description {
      font-size: 11px;
      margin-bottom: 18px;
    }
    
    .cta-button {
      padding: 8px 16px;
      font-size: 11px;
    }
  }

  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  @keyframes float {
    0%, 100% {
      transform: translateY(0px) scale(1);
      opacity: 0.1;
    }
    50% {
      transform: translateY(-25px) scale(1.1);
      opacity: 0.3;
    }
  }
</style>